import AdminOverview from "@/components/pages/admin/AdminOverview";

/** /admin — KPIs, the next booked call, and the merged activity feed. */
export default function AdminPage() {
  return <AdminOverview />;
}
