import AdminSlots from "@/components/pages/admin/AdminSlots";

/**
 * /admin/slots — open and close the half-hour slots the contact page offers.
 * Chrome, auth and the page `<main>` all come from the /admin layout's shell.
 */
export default function AdminSlotsPage() {
  return <AdminSlots />;
}
