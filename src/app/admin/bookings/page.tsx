import AdminBookings from "@/components/pages/admin/AdminBookings";

/** /admin/bookings — the roster of claimed calls, grouped by day. */
export default function AdminBookingsPage() {
  return <AdminBookings />;
}
